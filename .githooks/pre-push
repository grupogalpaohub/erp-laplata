#!/usr/bin/env bash
set -e
export GIT_STAGED_LIST="$(git diff --cached --name-only; echo; git diff --name-only)"
node scripts/guardrails/preflight.js || {
  echo "❌ Preflight reprovou. Corrija as violações acima e tente novamente."
  exit 2
}