# Temporariamente desabilitado para permitir deploy automático do Vercel
# name: Deploy Preview (erp-dev)

# on:
#   push:
#     branches: [ "erp-dev" ]

# jobs:
#   deploy-preview:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Trigger Vercel Preview Deploy via Hook
#         run: |
#           if [ -z "${{ secrets.VERCEL_DEPLOY_HOOK_PREVIEW }}" ]; then
#             echo "❌ Missing secret: VERCEL_DEPLOY_HOOK_PREVIEW"; exit 1
#           fi
#           curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_PREVIEW }}" \
#             -H 'Content-Type: application/json' \
#             -d "{\"payload\":{\"commit\":\"${{ github.sha }}\",\"branch\":\"${{ github.ref_name }}\",\"repo\":\"${{ github.repository }}\"}}"
