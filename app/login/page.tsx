export const runtime = 'edge';

export default function Page() {
  return (
    <div style={{display:'grid',gap:8}}>
      <h1>Login</h1>
      <p>Página de login carregada. (Depois conectamos com Supabase Auth.)</p>
    </div>
  );
}
