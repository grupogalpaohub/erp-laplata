==========================
CursorRules — Laplata ERP/CRM (v2, blindado)
==========================

Objetivo: impedir drift entre Supabase (DB), GitHub (código/migrations) e Cloudflare Pages (frontend), e proibir “atalhos” (NUNCA inventar dado/esquema). Qualquer desvio → PARE e peça autorização.

Database Rules (Supabase) — FONTE DE VERDADE

O Supabase (Postgres) é o schema oficial.

SEMPRE introspectar o Supabase (Supabase JS/pg) antes de gerar SQL, tipos ou modelos.

PROIBIDO inventar colunas/tabelas/enums/índices/PK/FK/triggers/views ou renomear campos “para facilitar”.

Mudanças de schema → OBRIGATORIAMENTE migration versionada em /db/migrations (formato VYYYYMMDD__descricao.sql).

Snapshot do schema: /db/schema.sql (commitado) — só atualizar após aplicar migrations.

Seeds iniciais: /db/initial_load.sql. Não inventar dados. Não criar “dados de teste” fora dos seeds.

Customizações (ENUM/Trigger/Índice/Function): /db/customizing.sql, idempotente.

PROIBIDO metacomandos psql (\d, \ir, \copy). Usar SQL puro compatível com Supabase.

Frontend Rules (Next.js em Cloudflare Pages/Workers)

Runtime Edge. Não usar APIs Node-only em Server Components (fs, path, process.cwd, crypto Node) — use APIs Web.

Supabase no cliente por padrão: createBrowserClient (SSR/server-client só se estritamente necessário e supportado no Edge).

PROIBIDO next export e qualquer artefato estático que intercepte rotas (ex.: public/index.html, public/login.html, out/**, .vercel/output/static/**).

Links absolutos no App Router: href="/mm/vendors", nunca relativos frágeis.

Cada rota deve existir via App Router com page.tsx (e loading.tsx/error.tsx para estados).

Rotas obrigatórias que devem abrir 200:

/setup

/login

/analytics

/co/dashboard /co/reports /co/costs

/mm/catalog /mm/vendors /mm/purchases

/sd /sd/orders /sd/customers /sd/invoices

/wh/inventory /wh/movements /wh/reports

/crm/leads /crm/opportunities /crm/activities

/fi/payables /fi/receivables /fi/cashflow

Harmonização (Supabase ↔ GitHub ↔ Cloudflare)

Antes de gerar código/queries, garantir que /db/schema.sql reflete o Supabase atual.

Se houver divergência:

criar migration em /db/migrations;

aplicar no Supabase;

introspectar novamente;

atualizar /db/schema.sql.

Erros de dados/colunas: ajustar o código ao schema existente; só propor DDL em migration se aprovado.

🔒 Anti-Simplificação (OBRIGATÓRIO)

PROIBIDO:

“Simplificar” removendo/renomeando campos/relacionamentos para passar build.

Criar dados fictícios, defaults mágicos, ou “exemplos” que não existam em /db/initial_load.sql.

Trocar ENUM por TEXT “temporariamente”.

Bypassar constraints/trigger/RLS “só para funcionar”.

Trocar tipos (ex.: bigint→text) sem migration formal.

Alterar policies RLS sem aprovação explícita.

PERMITIDO:

Diagnosticar causa raiz com referência ao schema real.

Propor correção via migration mínima, idempotente (com plano de rollback).

Ajustar queries e tipos do frontend para aderir ao schema sem mudar o banco.

SE DER ERRO:

Reportar a mensagem EXATA;

Explicar causa com tabela/coluna concreta;

Sugerir UMA via:

(A) ajuste de código para aderir ao schema;

(B) migration DDL mínima;

NUNCA “resolver” inventando dados ou mudando nomes.

Migrations & Idempotência

Migrations preferencialmente idempotentes (checar existência).

ENUM ADD VALUE em transação separada (COMMIT antes de usar).

Funções/Triggers: dropar/recriar sem CASCADE agressivo.

Seeds não alteram schema; se dependem de ENUM, garantir valor antes.

Qualidade & Segurança

Usar transações (BEGIN/COMMIT) em DDL/DML críticas.

Validar PK/UK/FK/triggers/enums após migrations (health-check SQL).

NUNCA expor segredos (Supabase/Cloudflare/Google). Usar .env e .env.example.

Logs/mensagens devem citar tabela/coluna e ação sugerida. Sem imprimir tokens.

GitHub Rules (repositório e commits)

Branch de produção: erp-git. Não force-push sem autorização.

PROIBIDO commitar out/**, .vercel/**, public/*.html (use .gitignore):

.vercel/
.next/
node_modules/
out/


Commits devem descrever o que e por que (ex.: “fix(pages): remover estáticos e garantir rotas mínimas”).

Nunca commitar .env com segredos reais.

Cloudflare Pages Rules (deploy e build)

Projeto: erp-laplata (nome fixo).

Build via Next on Pages:

Build command: npm ci && npm run build && npx @cloudflare/next-on-pages

Output dir: .vercel/output

Root directory:

frontend se o app estiver em /frontend;

vazio (raiz) se o app estiver na raiz.

Nunca subir arquivos estáticos no repo (Pages deve gerar o bundle no build remoto).

Verificação obrigatória após deploy: aba Functions deve listar todas as rotas. Se vazia → ainda há estático no repo ou root_dir errado.

Compat flags (quando aplicável):

compatibility_date: usar data atual;

compatibility_flags: ["nodejs_compat_populate_process_env"].

Fluxo “Seguro” (o Cursor deve seguir SEMPRE)

Limpar repo de artefatos estáticos:

Remover do Git (cached) out/**, .vercel/output/**, public/index.html, public/login.html (raiz e/ou frontend/).

Garantir .gitignore com .vercel/, .next/, out/, node_modules/.

Garantir rotas mínimas no App Router (incluir /sd e /login), loading.tsx e error.tsx.

Garantir next.config.js sem output:'export' e package.json com "build": "next build".

Ajustar Cloudflare Pages (se necessário):

Production branch = erp-git;

Root directory = frontend (se o app estiver lá) ou vazio se na raiz;

Build command / Output dir conforme acima.

Deploy (GitHub ou Direct Upload) sem expor segredos.

Validar:

Aba Functions lista as rotas?

URLs críticas retornam 200 (todas as listadas acima).

Se houver 404/503:

NUNCA criar index.html/login.html para “tapar buraco”.

Corrigir causa raiz (config do Pages, estático no repo, rota ausente).

Checklist (antes de abrir PR/merge/deploy)

 Código usa apenas colunas/tabelas/enum existentes no Supabase.

 Se schema mudou: migration em /db/migrations e snapshot /db/schema.sql atualizado.

 Sem mocks de dados no frontend (sem arrays/JSON “temporários”).

 Seeds somente em /db/initial_load.sql.

 Rotas obrigatórias existem em app/…/page.tsx (inclui /sd e /login).

 .gitignore bloqueia .vercel/, .next/, out/, node_modules/.

 Nenhum public/*.html e nenhum .vercel/output/static/*.html no repo.

 Cloudflare Pages configurado: branch, root_dir, build command, output dir.

 Deploy concluído com Functions listadas e URLs 200.
